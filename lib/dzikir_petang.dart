import 'package:flutter/material.dart';

class dzikir_petang extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Dzikir Petang'),
      ),
      body: Center(
        child: Text('Halaman Dzikir Petang'),
      ),
    );
  }
}
